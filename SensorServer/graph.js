// Produces graphs using chart.js for us on the web pages and the OLED
var chart = requir('chart');

// too tired to start this ... we dont have a document ...
// We can create a context to draw into.
// can also just append html to the stuff written as the result this will include the data to display (so this must be only what is needed for display (no more then on point per pixel!)
